//! `exocortex-server` library surface: the SSE hub router (§9.7) reused by
//! the `exocortex-node` binary and integration tests.

#![deny(unsafe_code)]
#![warn(missing_docs, rust_2018_idioms)]

pub mod sse;
