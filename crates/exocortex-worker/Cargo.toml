# crates/exocortex-worker/Cargo.toml
[package]
name    = "exocortex-worker"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Exocortex adapter host: runs external-source adapters (v1: noop) and forwards batches to the ingest service over gRPC. Never links the kernel."

[[bin]]
name = "exocortex-worker"
path = "src/main.rs"

[dependencies]
exocortex-wire = { workspace = true }
tonic = { workspace = true }
tokio = { workspace = true }
anyhow = { workspace = true }
clap = { version = "4", features = ["derive"] }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
